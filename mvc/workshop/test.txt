private void button1_Click_1(object sender, EventArgs e)
        {
            File.WriteAllText("test.txt", "Hello");
        }

        private void button2_Click_1(object sender, EventArgs e)
        {
            new Circle
            {
                Id = 1,
                R = 10,
            };
            Circle[] circle = new Circle[]
            {
                new Circle
                {
                    Id = 1,
                    R = 10,
                },
                new Circle
                {
                    Id = 2,
                    R = 45,
                },
                new Circle
                {
                    Id = 3,
                    R = 23,
                },
                new Circle
                {
                    Id = 4,
                    R = 67,
                },
            };
            List<Circle> circle2 = new List<Circle>()
            {
                 new Circle { },
                 new Circle { },
                 new Circle { },
            };
            listBox1.Items.AddRange(circle.ToArray());
        }

        private void button3_Click_1(object sender, EventArgs e)
        {
            string json;
            var options = new JsonSerializerOptions
            {
                WriteIndented = true,
            };
            json = JsonSerializer.Serialize(listBox1.Items, options);
            File.WriteAllText("test.json", json);

        }

        private void button4_Click_1(object sender, EventArgs e)
        {
            listBox1.Items.Clear();
            string json;
            json = File.ReadAllText("test.json");
            Circle[] circles = JsonSerializer.Deserialize(json, typeof(Circle[])) as Circle[];
            listBox1.Items.AddRange(circles.ToArray());
        }

        private void button5_Click_1(object sender, EventArgs e)
        {
            listBox1.Items.Clear();
        }

        private void button7_Click(object sender, EventArgs e)
        {
            XmlSerializer xmlSerializer = new XmlSerializer(typeof(Circle[]));
            Circle[] circles = listBox1.Items.Cast<Circle>().ToArray();
            StreamWriter streamWriter = new StreamWriter("test.txt");
            xmlSerializer.Serialize(streamWriter, circles);
        }

        private void button6_Click(object sender, EventArgs e)
        {
            XmlSerializer xmlSerializer = new XmlSerializer(typeof(Circle[]));
            StreamReader streamReader = new StreamReader("test.txt");
            Circle[] circles = xmlSerializer.Deserialize(streamReader) as Circle[];
            listBox1.Items.AddRange(circles.ToArray());
        }
